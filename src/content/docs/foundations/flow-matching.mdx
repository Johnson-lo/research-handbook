---
title: Flow Matching：Conditional Velocity 與 Marginal Velocity
description: 為什麼 e-x 是 sample-level supervision，而 v(z,t) 才是 marginal velocity field。
sidebar:
  order: 2
---

import VelocityGTExplorer from '../../../components/VelocityGTExplorer.astro';
import FlowMatchingNumericExample from '../../../components/FlowMatchingNumericExample.astro';

## 先不要背公式：用一組數字把 Flow Matching 算完

Flow Matching 最容易卡住的地方，是一開始就看到 $x$、$e$、$z_t$、$v$、MSE，卻不知道每個量到底「是什麼數字」。下面先固定一組 2D sample，把整個 training example 算一次。

<FlowMatchingNumericExample lang="zh" />

從這個例子先保留三件事：

1. training 直接抽 $x,e,t$ 後建構 $z_t$；
2. 對單一 sampled pair，target velocity 是 $e-x$；
3. 同一個 $(z_t,t)$ 若可能來自很多不同 pair，MSE 最後學到的是這些 conditional targets 的 conditional mean。

## 再回到一般公式

由

$$
z_t=(1-t)x+te
$$

可以得到

$$
\frac{dz_t}{dt}=e-x.
$$

對某一組已抽樣的 $(x,e)$ 而言，$e-x$ 是這組 sample-specific path 的 **conditional instantaneous velocity**。

但同一個 $(z_t,t)$ 可能對應到很多不同的 $(x,e)$。因此，在 MSE regression 下唯一的最優 field 是

$$
v(z_t,t)=\mathbb E[e-x\mid z_t,t].
$$

<VelocityGTExplorer lang="zh" />

## 三個不能混在一起的「GT」層級

| 層級 | Quantity | 角色 |
|---|---|---|
| Sample supervision | $e-x$ | 某一組 sampled pair 的 conditional velocity |
| Unique instantaneous field | $v(z,t)$ | Marginal velocity，也是 MSE regression optimum |
| MeanFlow theoretical field | $u(z,r,t)$ | 一段 interval 上的 average velocity |

Flow Matching 的 training objective 是

$$
\mathcal L_{FM}=\mathbb E\|v_\theta(z_t,t)-(e-x)\|^2.
$$

Inference 時則從 $t=1$ 往 $t=0$ 數值求解 $dz_t/dt=v_\theta(z_t,t)$；由於每到新的 state 通常都要重新 query instantaneous velocity，所以一般需要 multiple NFEs。
