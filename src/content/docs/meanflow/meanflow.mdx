---
title: MeanFlow 深入｜Average Velocity、Identity 與 JVP
description: MeanFlow average velocity、Identity 與 JVP 的數學結構。
sidebar:
  order: 1
---

import JVPExplorer from '../../../components/JVPExplorer.astro';
import DerivationStepper from '../../../components/DerivationStepper.astro';

:::note[章節定位]
本頁聚焦 MeanFlow 的數學結構；完整方法脈絡可參考 [完整故事線](/research-handbook/meanflow/story/)。
:::

## Average velocity

$$
u(z_t,r,t)=\frac{1}{t-r}\int_r^t v(z_\tau,\tau)\,d\tau.
$$

因此

$$
(t-r)u=\int_r^t v(z_\tau,\tau)\,d\tau,
$$

以及

$$
z_r=z_t-(t-r)u(z_t,r,t).
$$

One-step generation：

$$
(r,t)=(0,1),\qquad z_0=z_1-u_\theta(z_1,0,1).
$$

## MeanFlow Identity 的推導

<DerivationStepper lang="zh" />

最後得到

$$
v=u+(t-r)\frac{du}{dt}.
$$

Total derivative：

$$
\frac{du}{dt}=\partial_z u\,v+\partial_tu=\operatorname{JVP}(u;v).
$$

<JVPExplorer lang="zh" />

## Original MeanFlow objective

$$
u_{tgt}=(e-x)-(t-r)\operatorname{JVP}(u_\theta;e-x).
$$

$$
\mathcal L=\mathbb E\|u_\theta-\operatorname{sg}(u_{tgt})\|^2.
$$

這裡 `sg` 表示 stop-gradient。$u_\theta$ 的語意仍是 average velocity；$e-x$ 是 sample-specific conditional velocity supervision，並在 Original MeanFlow 中作為 JVP tangent 的近似。
