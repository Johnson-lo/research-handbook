---
title: Improved MeanFlow 深入｜Objective Reformulation 與其他系統改動
description: 把 objective-level change 與 CFG/architecture changes 分開理解。
sidebar:
  order: 2
---

import MFvsIMFBridge from '../../../components/MFvsIMFBridge.astro';

:::note[先抓核心]
如果兩篇 paper 的差異還不直覺，先看下面的比較元件。**不要先背 1.72 FID，也不要先背 CFG。**
:::

<MFvsIMFBridge lang="zh" />

## 1. Original MF 的 equivalent v-loss view

由 MeanFlow Identity：

$$
v=u+(t-r)\frac{du}{dt}
$$

定義

$$
V_\theta=u_\theta+(t-r)\operatorname{JVP}_{sg}(u_\theta;e-x).
$$

Original MF 可以等價看成

$$
\mathcal L=\mathbb E\|V_\theta-(e-x)\|^2.
$$

這個 view 暴露出 predictor 實際是

$$
V_\theta(z_t,e-x),
$$

因為 sample-specific $e-x$ 進入 JVP tangent。

## 2. iMF 的 objective-level change

iMF 改成

$$
V_\theta(z_t)=u_\theta(z_t)+(t-r)\operatorname{JVP}_{sg}(u_\theta;v_\theta).
$$

但 supervision 保持

$$
\mathcal L=\mathbb E\|V_\theta(z_t)-(e-x)\|^2.
$$

:::caution[最重要的避免誤解]
iMF **沒有**移除 $e-x$ supervision。它移除的是 $e-x$ 直接進入 JVP / predictor pathway。
:::

## 3. $v_\theta$ 從哪裡來？

Boundary condition：

$$
v(z_t,t)=u(z_t,t,t),
$$

因此可用同一個 network 定義

$$
v_\theta(z_t,t)\equiv u_\theta(z_t,t,t).
$$

Paper 也研究 training-only auxiliary $v$-head。

## 4. 不要和 objective change 混在一起的其他 iMF 改動

- random $\omega$ conditioning
- $\Omega=\{\omega,t_{min},t_{max}\}$ conditioning
- in-context conditioning
- 移除 adaLN-zero
- SwiGLU / RMSNorm / RoPE
- longer training

因此 final FID 1.72 是 system-level result，不是 objective-only result。
