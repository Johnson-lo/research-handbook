---
title: MeanFlow Deep Dive | Average Velocity, Identity, and JVP
description: A mathematical deep dive after the main story is clear.
sidebar:
  order: 1
---

import JVPExplorer from '../../../../components/JVPExplorer.astro';
import DerivationStepper from '../../../../components/DerivationStepper.astro';

:::note[Recommended order]
On a first read, start with [The Full Story](/research-handbook/en/meanflow/story/). This page is a mathematical deep dive rather than a from-scratch narrative.
:::

## Average velocity

$$
u(z_t,r,t)=\frac{1}{t-r}\int_r^t v(z_\tau,\tau)\,d\tau.
$$

Thus

$$
(t-r)u=\int_r^t v(z_\tau,\tau)\,d\tau,
$$

and

$$
z_r=z_t-(t-r)u(z_t,r,t).
$$

For one-step generation:

$$
(r,t)=(0,1),\qquad z_0=z_1-u_\theta(z_1,0,1).
$$

## Deriving the MeanFlow Identity

<DerivationStepper lang="en" />

The result is

$$
v=u+(t-r)\frac{du}{dt}.
$$

The total derivative is

$$
\frac{du}{dt}=\partial_z u\,v+\partial_tu=\operatorname{JVP}(u;v).
$$

<JVPExplorer lang="en" />

## Original MeanFlow objective

$$
u_{tgt}=(e-x)-(t-r)\operatorname{JVP}(u_\theta;e-x),$$

$$
\mathcal L=\mathbb E\|u_\theta-\operatorname{sg}(u_{tgt})\|^2.
$$

The key distinction is that $u_\theta$ does not mean $e-x$. It remains an average velocity; $e-x$ is sample-specific conditional velocity supervision and is used by Original MF as the JVP tangent approximation.
